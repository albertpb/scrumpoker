# Optional WebSocket base URL for a remote backend.
# Local development automatically uses ws://localhost:8080.
# VITE_WS_URL=wss://api.example.com

# BACKEND_URL is a frontend container runtime variable, not a Vite variable.
# Pass it with docker run rather than placing it in this file.
# BACKEND_URL=http://backend:8080
